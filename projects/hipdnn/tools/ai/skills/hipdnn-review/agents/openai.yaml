interface:
  display_name: "hipDNN Review (deprecated)"
  short_description: "Deprecated — use hipdnn-pr-quality"
  default_prompt: "DEPRECATED: hipdnn-review has been replaced by $hipdnn-pr-quality. First tell the user this skill is deprecated, then use $hipdnn-pr-quality (review assist) to review the hipDNN PR, branch, or diff."
